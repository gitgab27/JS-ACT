var totalpricedisplay = function() {
var total_price = parseInt(document.getElementById("Magic").value) + parseInt(document.getElementById("metal").value);

return alert("Total Price of Item" + total_price);

}